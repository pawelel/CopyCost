<UserControl x:Class="CopyCost.WPF.Category.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CopyCost.WPF.Category"
             mc:Ignorable="d">
    <Grid Background="Aquamarine">
        <ContentControl Grid.Column="0" d:DataContext="{d:DesignInstance Type=local:CategoryViewModel}">
            <ListView ItemsSource="{Binding Categories}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Padding="10"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ContentControl>
    </Grid>
</UserControl>
